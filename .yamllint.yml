extends: default
rules:
  line-length: disable
  truthy: disable
  comments-indentation: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  braces:
    max-spaces-inside: 1
