apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: alertmanager-heartbeat
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: alertmanager-heartbeat
  data:
    - secretKey: url
      remoteRef:
        key: infrastructure/alertmanager-heartbeat
        property: url
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
