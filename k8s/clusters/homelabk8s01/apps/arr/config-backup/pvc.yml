apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: arr-config-backups
  namespace: arr
  labels:
    app.kubernetes.io/name: arr-config-backup
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 2Gi
