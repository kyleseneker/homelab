apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: arr-seerr-config
  namespace: arr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
