apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: arr-prowlarr-config
  namespace: arr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
