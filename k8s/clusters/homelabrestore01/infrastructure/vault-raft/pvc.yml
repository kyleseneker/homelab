apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restore-vault-raft
  namespace: restore-vault
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
