apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restore-tdarr
  namespace: restore-tdarr
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
