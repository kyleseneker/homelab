apiVersion: v1
kind: Service
metadata:
  name: restore-indexer
  namespace: restore-sonarr
spec:
  selector:
    app.kubernetes.io/name: restore-indexer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
