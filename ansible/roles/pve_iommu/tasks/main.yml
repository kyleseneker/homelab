---
- name: Set GRUB cmdline for IOMMU passthrough
  ansible.builtin.lineinfile:
    path: /etc/default/grub
    regexp: '^GRUB_CMDLINE_LINUX_DEFAULT='
    line: 'GRUB_CMDLINE_LINUX_DEFAULT="{{ pve_iommu_grub_cmdline }}"'
  register: pve_iommu_grub_updated

- name: Update GRUB # noqa: no-handler
  ansible.builtin.command: update-grub
  changed_when: true
  when: pve_iommu_grub_updated.changed

- name: Ensure VFIO modules are loaded on boot
  ansible.builtin.lineinfile:
    path: /etc/modules
    line: "{{ item }}"
    create: true
    mode: "0644"
  loop: "{{ pve_iommu_vfio_modules }}"
  register: pve_iommu_modules_updated

- name: Reboot to apply IOMMU and VFIO changes
  ansible.builtin.reboot:
    reboot_timeout: 300
    msg: "Rebooting to enable IOMMU and VFIO modules"
  when: pve_iommu_grub_updated.changed or pve_iommu_modules_updated.changed
